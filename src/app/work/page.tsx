import type { Metadata } from 'next';
import Portfolio from '@/components/Portfolio';
export const metadata: Metadata = { title: 'Selected work', description: 'Explore AI, analytics, and integration work by Iver Macaulay.', alternates: { canonical: '/work' } };
export default Portfolio;
