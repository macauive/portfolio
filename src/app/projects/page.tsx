import type { Metadata } from 'next';
import Portfolio from '@/components/Portfolio';
export const metadata: Metadata = { title: 'Projects', description: 'Independent products and project details by Iver Macaulay.', alternates: { canonical: '/projects' } };
export default Portfolio;
